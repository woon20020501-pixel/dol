[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = true

[fuzz]
runs = 256

[invariant]
runs = 64
depth = 32

# FFI enabled for deploy scripts (writes shared/contracts.json via Node.js)
ffi = true
